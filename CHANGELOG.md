# Changelog

## v0.1.0 — Initial Release (November 2025)

- Implemented core Go API with `/v1/flags`, `/v1/flags/snapshot`, and SSE `/v1/flags/stream`
- Added TypeScript client SDK with caching and live updates
- Created Admin UI (HTML + JS) with real-time refresh via SDK
- Basic authentication via Bearer key
